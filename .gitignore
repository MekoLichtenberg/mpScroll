# Workshop-Daten NIEMALS ins öffentliche Repo:
# enthält hochgeladene Videos/Profilbilder (evtl. mit Teilnehmenden) und die aktuelle PIN.
data/

# Abhängigkeiten und Logs
node_modules/
*.log

# Fertige Pakete + heruntergeladene Node-Runtimes (werden per tools/build-kit.mjs erzeugt)
dist/
.build-cache/
runtime/

# Betriebssystem
.DS_Store
Thumbs.db
